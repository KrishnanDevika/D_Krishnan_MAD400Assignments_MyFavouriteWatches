import { WatchTypesPipe } from './watch-types.pipe';

describe('WatchTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new WatchTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
